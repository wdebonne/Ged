{
  "name": "ged-courrier",
  "version": "1.0.0",
  "description": "Application de Gestion Électronique de Courrier",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "keywords": ["ged", "courrier", "gestion", "documents"],
  "author": "Service Informatique de Pavilly",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
